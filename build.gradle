group 'com.automation.core'
version '1.0-SNAPSHOT'

subprojects {

    repositories {
        mavenCentral()
    }

    defaultTasks 'check', 'test', 'install'

    configurations {
        agent
    }
}